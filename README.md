## Example for react experimental example

This example shows how to use the `use` hook to load data from an external API.


*care*
```json
// react version
{
   "dependencies": {
      "react": "experimental",
      "react-dom": "experimental"
   }
}
```